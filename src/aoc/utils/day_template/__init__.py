import logging
from pathlib import Path

log = logging.getLogger(__name__)
_debug = False  ## Whether we are in debug mode


def solve_first(input_file: Path) -> str | None:
    log.debug(f"{input_file=}")
    log.error("run_first: Not implemented")
    return None


def solve_second(input_file: Path) -> str | None:
    log.debug(f"{input_file=}")
    log.error("run_second: Not implemented")
    return None
